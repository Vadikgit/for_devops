name: Build and Push Server Docker Image, restart Server Kubernetes Deployment

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install And Set up yc-cli
        run: |
          echo 2222222222222222
